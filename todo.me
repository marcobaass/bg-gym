[x] Boardrender point 1 is top right not bottom right
[x] Offset of the top row
[x] Stack when more than 5 on pointg
[x] show checkers on bar
[x] show dice
[x] show checkers off board ( boardframe new size checken - versatz checker)
[x] show doubling cube value
[x] show score
[x] show pips
[x] Let points hover and click, but only top checker of stacks // pointIndex?!?!
[x] highlight all checkers on one point
[x] show destination point (green dot)
[X] behaviour of hover and click and next position
[x] destination from bar
[x] bear off indicator
[ ] whole move logic
    [x] remove checker from array and gray out
    [x] move checker
    [x] store moves
    [X] bear off checker
    [x] enter bar checker
    [x] hit checker
[x] correct parser - transform annotation string in array with best moves
    now: [[-2, 20][8, 2]] or [[9, 4]]
[x] check vs. top 5 moves ui.moves array vs. bestmoves array
[x] next position flow
[x] Handle edge case: user's move not found in bestMoves at all
[x] point system (best move 5 points etc...)
[x] when parsing check for forced move
[x] when cube action move
    [x] tabs for parser move/cube
    [x] parser for cube action
    [x] error messages on submit analysis
[ ] add numbers(notation) to the board
[ ] undo
[ ] what happens when user is through all positions? ResultModal greyed out button
    some final result with how much wrong or right stats?
    option for setting how much positions to train
[ ] when parsing option to choose which category (new ones or existing ones)
[ ] authentification and database implementation

Yes, perfect time to finish the results modal for cubes.
Let’s keep going in small, concrete steps.
Next task: build cubeOptions rows in handleSubmitCubeDecision
Right now, in handleSubmitCubeDecision you:
Compute pointsForDecision.
Call setCubePoints(pointsForDecision) and dispatch(...).
But you don’t yet build cubeOptions for the modal.
Your task:
In handleSubmitCubeDecision, after you’ve decided bestEquity and userEquity and computed pointsForDecision (i.e. just before the console.log lines), build an array of CubeOptionRow using summary.options, like this in words:
Start from summary.options.
Sort them by equity descending (best first).
Map each option to a CubeOptionRow:
rank: index + 1
label: use getCubeButtonLabel(opt.decision, isRedouble)
equity: opt.equity
equityDiff: Math.abs(bestEquity - opt.equity)
isUserOption: opt.decision === cubeDecision
Store the result in a local rows variable.
Call setCubeOptions(rows); right after you compute them.
Also call setShowResultsModal(true); so the existing ResultsModal opens for cube decisions too.
Don’t change ResultsModal itself yet; just wire up rows and setShowResultsModal.
When you’ve done that and clicked a cube decision, tell me what the modal shows (roughly), and we’ll clean up any remaining details there next.